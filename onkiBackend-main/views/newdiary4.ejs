<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title></title>
        <link rel="stylesheet" href="../styles/newdiary4.css">
    </head>

    <body>
        <main>
          
            <div class="light">
            </div>

            <img src="../assets/onki.svg" class="onki"/>

            <div class="complete">일기장 생성이 완료되었어요!</div>
            <img class="celebrate" src="../assets/celebrate.png" />
            <button class = "newprofile" onClick="location.href='newdiary5.html'">프로필 생성하기</button>
            <input type="hidden" id="diaryId" value="<%= diaryId %>">
            <script>
                // JavaScript 코드 없이 버튼 클릭 이벤트 처리

                const diaryId = window.location.pathname.split('/').pop();
                document.querySelector('.newprofile').addEventListener('click', function() {
                    window.location.href = `/newdiary5/${diaryId}`;
                });
            </script>

        </main>
    </body>
</html>  